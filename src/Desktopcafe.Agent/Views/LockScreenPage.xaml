<UserControl
    x:Class="Desktopcafe.Agent.Views.LockScreenPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        RequestedTheme="Dark">
    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="24"
        MaxWidth="400">

      <TextBlock
          Text="Desktopcafe"
          FontSize="48"
          FontWeight="Bold"
          HorizontalAlignment="Center"
          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />

      <FontIcon
          Glyph="&#xE7F4;"
          FontSize="64"
          HorizontalAlignment="Center"
          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

      <TextBlock
          x:Name="ComputerNameText"
          Text="PC-01"
          FontSize="24"
          FontWeight="SemiBold"
          HorizontalAlignment="Center" />

      <Border
          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          CornerRadius="12"
          Padding="32">
        <StackPanel Spacing="12" HorizontalAlignment="Center">
          <FontIcon Glyph="&#xE72E;" FontSize="32" HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
          <TextBlock
              x:Name="LockMessageText"
              Text="Esta computadora esta bloqueada"
              FontSize="16"
              TextAlignment="Center"
              TextWrapping="Wrap" />
          <TextBlock
              Text="Solicite acceso en el mostrador"
              FontSize="14"
              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
              TextAlignment="Center" />
        </StackPanel>
      </Border>

      <Border
          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
          CornerRadius="8"
          Padding="16">
        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
          <FontIcon Glyph="&#xE701;" FontSize="20" />
          <StackPanel>
            <TextBlock Text="WiFi disponible" FontWeight="SemiBold" />
            <TextBlock Text="Pregunte por un voucher de acceso"
                       FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
          </StackPanel>
        </StackPanel>
      </Border>

      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
        <TextBlock x:Name="ClockText" Text="10:30 AM" FontSize="14"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <TextBlock x:Name="DateText" Text="05/03/2026" FontSize="14"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
      </StackPanel>

      <!-- Connection status -->
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
        <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="Green" />
        <TextBlock x:Name="StatusText" Text="Conectado" FontSize="11"
                   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
      </StackPanel>
    </StackPanel>
  </Grid>
</UserControl>
