<Page
    x:Class="Desktopcafe.Server.Views.InventoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

  <Grid Padding="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <StackPanel Orientation="Horizontal" Spacing="16">
      <TextBlock Text="Inventario" Style="{StaticResource PageHeaderStyle}" />
      <Button x:Name="AddProductBtn" Content="Agregar Producto" Style="{StaticResource AccentButtonStyle}" Click="AddProduct_Click">
        <Button.Icon><FontIcon Glyph="&#xE710;" /></Button.Icon>
      </Button>
    </StackPanel>

    <!-- Low Stock Alert -->
    <InfoBar Grid.Row="1" x:Name="LowStockAlert" Title="Alerta de Stock Bajo"
             Severity="Warning" IsClosable="False" IsOpen="False" Margin="0,0,0,12" />

    <!-- Search -->
    <AutoSuggestBox Grid.Row="2" x:Name="SearchBox" PlaceholderText="Buscar producto..."
                     QueryIcon="Find" Width="350" HorizontalAlignment="Left" Margin="0,0,0,12"
                     TextChanged="SearchBox_TextChanged" />

    <!-- Product List -->
    <ListView Grid.Row="3" x:Name="ProductList" SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource KpiCardStyle}" Margin="0,4">
            <Grid ColumnSpacing="16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" Text="{Binding Category}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
              <TextBlock Grid.Column="2" Text="{Binding PriceText}" FontWeight="SemiBold" VerticalAlignment="Center" />
              <TextBlock Grid.Column="3" Text="{Binding StockText}" VerticalAlignment="Center"
                         Foreground="{Binding StockColor}" />
              <TextBlock Grid.Column="4" Text="{Binding MinStockText}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

              <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="4">
                <Button ToolTipService.ToolTip="Editar" Tag="{Binding Id}" Click="EditProduct_Click">
                  <FontIcon Glyph="&#xE70F;" FontSize="14" />
                </Button>
                <Button ToolTipService.ToolTip="Agregar Stock" Tag="{Binding Id}" Click="AddStock_Click">
                  <FontIcon Glyph="&#xE710;" FontSize="14" />
                </Button>
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
