<Page
    x:Class="Desktopcafe.Server.Views.WiFiPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

  <Grid Padding="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Vouchers WiFi" Style="{StaticResource PageHeaderStyle}" />

    <!-- Generate Voucher -->
    <Border Grid.Row="1" Style="{StaticResource KpiCardStyle}" Margin="0,0,0,16">
      <StackPanel Orientation="Horizontal" Spacing="16">
        <NumberBox x:Name="DurationBox" Header="Duracion (minutos)" Value="60" Minimum="1"
                   SpinButtonPlacementMode="Compact" Width="200" />
        <Button Content="Generar Voucher" Style="{StaticResource AccentButtonStyle}" Click="GenerateVoucher_Click"
                VerticalAlignment="Bottom">
          <Button.Icon><FontIcon Glyph="&#xE710;" /></Button.Icon>
        </Button>
      </StackPanel>
    </Border>

    <!-- Voucher List -->
    <ListView Grid.Row="2" x:Name="VoucherList" SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource KpiCardStyle}" Margin="0,4">
            <Grid ColumnSpacing="16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>

              <TextBlock Text="{Binding Code}" FontFamily="Consolas" FontSize="18" FontWeight="Bold"
                         VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" Text="{Binding DurationText}" VerticalAlignment="Center" />
              <TextBlock Grid.Column="2" Text="{Binding StatusText}" VerticalAlignment="Center"
                         Foreground="{Binding StatusColor}" />
              <TextBlock Grid.Column="3" Text="{Binding CreatedText}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
