<Page
    x:Class="Desktopcafe.Server.Views.ReservationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

  <Grid Padding="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Reservaciones" Style="{StaticResource PageHeaderStyle}" />

    <!-- New Reservation -->
    <Border Grid.Row="1" Style="{StaticResource KpiCardStyle}" Margin="0,0,0,16">
      <StackPanel Spacing="12">
        <TextBlock Text="Nueva Reservacion" Style="{StaticResource SectionHeaderStyle}" />
        <StackPanel Orientation="Horizontal" Spacing="16">
          <ComboBox x:Name="ComputerCombo" Header="Computadora" Width="200" />
          <CalendarDatePicker x:Name="ReservationDate" Header="Fecha" PlaceholderText="Seleccionar fecha" />
          <TimePicker x:Name="ReservationTime" Header="Hora" ClockIdentifier="24HourClock" />
          <NumberBox x:Name="DurationBox" Header="Duracion (min)" Value="60" Minimum="15"
                     SpinButtonPlacementMode="Compact" Width="150" />
          <Button Content="Reservar" Style="{StaticResource AccentButtonStyle}" Click="CreateReservation_Click"
                  VerticalAlignment="Bottom">
            <Button.Icon><FontIcon Glyph="&#xE710;" /></Button.Icon>
          </Button>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Reservations List -->
    <ListView Grid.Row="2" x:Name="ReservationList" SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource KpiCardStyle}" Margin="0,4">
            <Grid ColumnSpacing="16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <TextBlock Text="{Binding ComputerName}" FontWeight="SemiBold" VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" Text="{Binding StartTimeText}" VerticalAlignment="Center" />
              <TextBlock Grid.Column="2" Text="{Binding DurationText}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
              <TextBlock Grid.Column="3" Text="{Binding StatusText}" VerticalAlignment="Center"
                         Foreground="{Binding StatusColor}" />

              <Button Grid.Column="4" ToolTipService.ToolTip="Cancelar" Tag="{Binding Id}" Click="CancelReservation_Click"
                      Visibility="{Binding CanCancel}">
                <FontIcon Glyph="&#xE711;" FontSize="14" Foreground="{StaticResource StatusErrorBrush}" />
              </Button>
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
