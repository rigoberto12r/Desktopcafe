<Page
    x:Class="Desktopcafe.Server.Views.EmployeesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

  <Grid Padding="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <StackPanel Orientation="Horizontal" Spacing="16">
      <TextBlock Text="Empleados" Style="{StaticResource PageHeaderStyle}" />
      <Button x:Name="AddEmployeeBtn" Content="Agregar Empleado" Style="{StaticResource AccentButtonStyle}" Click="AddEmployee_Click">
        <Button.Icon><FontIcon Glyph="&#xE710;" /></Button.Icon>
      </Button>
    </StackPanel>

    <!-- Employee List -->
    <ListView Grid.Row="1" x:Name="EmployeeList" SelectionMode="None">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource KpiCardStyle}" Margin="0,4">
            <Grid ColumnSpacing="16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" Text="{Binding Username}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
              <TextBlock Grid.Column="2" Text="{Binding RoleText}" VerticalAlignment="Center" />
              <TextBlock Grid.Column="3" Text="{Binding StatusText}" VerticalAlignment="Center"
                         Foreground="{Binding StatusColor}" />

              <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="4">
                <ToggleSwitch Tag="{Binding Id}" IsOn="{Binding IsActive}" Toggled="ToggleActive_Toggled"
                              OnContent="Activo" OffContent="Inactivo" />
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
