<Page
    x:Class="Desktopcafe.Server.Views.ClientsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

  <Grid Padding="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <StackPanel Orientation="Horizontal" Spacing="16">
      <TextBlock Text="Clientes" Style="{StaticResource PageHeaderStyle}" />
      <Button x:Name="AddClientBtn" Content="Agregar Cliente" Style="{StaticResource AccentButtonStyle}" Click="AddClient_Click">
        <Button.Icon><FontIcon Glyph="&#xE710;" /></Button.Icon>
      </Button>
    </StackPanel>

    <!-- Search -->
    <AutoSuggestBox Grid.Row="1" x:Name="SearchBox" PlaceholderText="Buscar por nombre, telefono o codigo..."
                     QueryIcon="Find" Width="400" HorizontalAlignment="Left" Margin="0,0,0,12"
                     TextChanged="SearchBox_TextChanged" />

    <!-- Client List -->
    <ListView Grid.Row="2" x:Name="ClientList" SelectionMode="Single">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource KpiCardStyle}" Margin="0,4">
            <Grid ColumnSpacing="16">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="140" />
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
              <TextBlock Grid.Column="1" Text="{Binding Phone}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
              <TextBlock Grid.Column="2" Text="{Binding Email}" VerticalAlignment="Center"
                         Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
              <TextBlock Grid.Column="3" Text="{Binding Code}" VerticalAlignment="Center" FontFamily="Consolas" />
              <TextBlock Grid.Column="4" Text="{Binding BalanceText}" VerticalAlignment="Center" FontWeight="SemiBold" />
              <TextBlock Grid.Column="5" Text="{Binding PointsText}" VerticalAlignment="Center" />

              <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="4">
                <Button ToolTipService.ToolTip="Editar" Tag="{Binding Id}" Click="EditClient_Click">
                  <FontIcon Glyph="&#xE70F;" FontSize="14" />
                </Button>
                <Button ToolTipService.ToolTip="Recargar Saldo" Tag="{Binding Id}" Click="RechargeBalance_Click">
                  <FontIcon Glyph="&#xE8C7;" FontSize="14" />
                </Button>
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
